## Arvik v0.9.5 — Performance Sprint

Arvik v0.9.5 is the performance-focused release: low-level socket and listener
tuning, HTTP/2 presets, lower-copy `Body` constructors, and a manual benchmark
harness for measuring all of it.

### Highlights

- Low-level socket/listener tuning on `ServerConfig`: TCP_NODELAY, keepalive,
  reuse port/address, backlog, socket buffers, and accept worker settings.
- HTTP/2 performance presets: `ServerConfig::http2_low_latency()` and
  `ServerConfig::http2_high_throughput()`.
- `RuntimeConfig` for tuned Tokio runtime construction.
- Optional `runtime-metrics` facade feature forwarding to
  `arvik-hyper/runtime-metrics`.
- Lower-copy `Body` constructors: `from_static`, `from_chunks`, and
  `is_empty_hint`.
- Benchmark examples under `examples/benchmarks` and a tuning example under
  `examples/performance_tuning`.
- Manual benchmark workflow that uploads benchmark artifacts.
- `scripts/bench/wrk.sh`, `scripts/bench/hey.sh`, and
  `scripts/bench/run-all.sh`.

### Changed

- Router matched paths now use interned `Arc<str>` metadata.
- Path parameter keys use stack-backed `SmallVec` storage with interned names.
- JSON responses serialize into `BytesMut` and freeze into `Bytes`.
- Arvik config maps socket/listener settings into `ServerConfig`.
- Workspace version bumped to `0.9.5`.

### Notes

- The planned full Linux `io_uring` backend is deferred as future experimental
  work. The 0.9.x completion path is maintainable Tokio/socket tuning plus
  optional runtime metrics.
- Benchmark CI is manual and artifact-based, not PR-commenting or release
  gating.

### Compatibility

No public Rust API was removed or renamed. New `ServerConfig` methods are
additive; existing configurations continue to work unchanged.

### Install

```toml
[dependencies]
arvik = "0.9.5"
```

With TLS, WebSocket, and configuration support:

```toml
[dependencies]
arvik = { version = "0.9.5", features = ["tls", "ws", "config"] }
```

### Crates published to crates.io

| Crate | Version | Description |
|-------|---------|-------------|
| `arvik-core` | `0.9.5` | Core traits, request/response types, extractors |
| `arvik-router` | `0.9.5` | Radix-trie router and method dispatch |
| `arvik-hyper` | `0.9.5` | Hyper-based server with graceful shutdown |
| `arvik-extract` | `0.9.5` | Body/multipart/cookie extractors |
| `arvik-middleware` | `0.9.5` | CORS, auth, rate limiting, compression |
| `arvik-ws` | `0.9.5` | WebSocket upgrades and sockets |
| `arvik-sse` | `0.9.5` | Server-sent events |
| `arvik-static` | `0.9.5` | Static file and embedded asset serving |
| `arvik-tls` | `0.9.5` | rustls + native-tls backends |
| `arvik-macros` | `0.9.5` | Route and handler proc macros |
| `arvik-test` | `0.9.5` | Test client utilities |
| `arvik-config` | `0.9.5` | File/env runtime configuration |
| `arvik-observe` | `0.9.5` | Metrics, logging, tracing, health checks |
| `arvik` | `0.9.5` | Stable facade crate |

### Documentation

- [README](../../README.md)
- [Architecture](../../ARCHITECTURE.md)
- [Roadmap](../../ROADMAP.md)
- [Changelog](../../CHANGELOG.md)
