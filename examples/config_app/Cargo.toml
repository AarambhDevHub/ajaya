[package]
name = "config_app"
version = "0.1.0"
edition = "2024"

[dependencies]
arvik = { path = "../../arvik", features = ["config"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
