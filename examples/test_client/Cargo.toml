[package]
name = "test_client"
version = "0.1.0"
edition = "2024"

[dependencies]
arvik = { path = "../../arvik", features = ["test-utils"] }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
